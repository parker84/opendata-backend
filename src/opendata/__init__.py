"""opendata — the open-source data workspace in the AI era.

This package is the v1 vertical slice described in docs/architecture.md:
connectors → context store → answer engine → CLI, runnable offline against the
bundled toy dbt + DuckDB fixture in examples/toy.
"""

__version__ = "0.1.0"
